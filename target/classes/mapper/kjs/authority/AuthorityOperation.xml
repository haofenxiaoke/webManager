<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kjs.orm.mapper.AuthorityOperationMapper">
	<resultMap type="AuthorityOperation" id="authorityOperationResultMap">
		<result property="id" column="id" />
		<result property="name" column="name" />
		<result property="code" column="code" />
		<result property="description" column="description" />
		<result property="menuId" column="menu_id" />
		<result property="lastModifyUserId" column="last_modify_user_id" />
		<result property="lastModifyDatetime" column="last_modify_datetime" />
	</resultMap>
	
	<sql id="table_columns">
		id,
		name,
		code,
		description,
		menu_id,
		last_modify_user_id,
		last_modify_datetime
    </sql>
	<sql id="entity_properties">
		#{id},
		#{name},
		#{code},
		#{description},
		#{menuId},
		#{lastModifyUserId},
		#{lastModifyDatetime}
	</sql>
	
	<!-- 适用于主键自增类型 -->
	<insert id="save" parameterType="AuthorityOperation" useGeneratedKeys="true" keyProperty="long">
		insert into authority_operation( <include refid="table_columns" /> ) 
		values ( <include refid="entity_properties" /> )
	</insert>
	
	<delete id="deleteByIds">
		delete from authority_operation
		where id in
		<foreach item="item" collection="array" open="(" separator="," close=")">
			#{item}
		</foreach>
	</delete>
	
	<update id="update" parameterType="AuthorityOperation">
		update authority_operation 
		<trim prefix="set" suffixOverrides=",">
			<if test="name != null and name != ''">name = #{name},</if>
			<if test="code != null and code != ''">code = #{code},</if>
			<if test="description != null and description != ''">description = #{description},</if>
			<if test="menuId != null and menuId != ''">menu_id = #{menuId},</if>
			<if test="lastModifyUserId != null and lastModifyUserId != ''">last_modify_user_id = #{lastModifyUserId},</if>
			<if test="lastModifyDatetime != null and lastModifyDatetime != ''">last_modify_datetime = #{lastModifyDatetime},</if>
		</trim>
		<where>id = #{id}</where>
	</update>
	
	<select id="findById" resultMap="authorityOperationResultMap" parameterType="long" >
		select <include refid="table_columns" />
		from authority_operation
		where id = #{id}
	</select>
	
	<select id="queryAll" resultMap="authorityOperationResultMap">
		select <include refid="table_columns" /> 
		from authority_operation
	</select>
	
	<!-- 使用like用法：columnName like concat('%',#columnName#,'%') -->
	<sql id="page_where">
		<trim prefix="where" prefixOverrides="and | or ">
			<if test="name != null and name != ''">and name = #{name}</if>
			<if test="code != null and code != ''">and code = #{code}</if>
			<if test="description != null and description != ''">and description = #{description}</if>
			<if test="menuId != null and menuId != ''">and menu_id = #{menuId}</if>
			<if test="lastModifyUserId != null and lastModifyUserId != ''">and last_modify_user_id = #{lastModifyUserId}</if>
			<if test="lastModifyDatetime != null and lastModifyDatetime != ''">and last_modify_datetime = #{lastModifyDatetime}</if>
		</trim>
	</sql>
	
	<select id="search" resultMap="authorityOperationResultMap" parameterType="AuthorityOperation">
		select <include refid="table_columns" />
		from authority_operation
		<include refid="page_where" />
	</select>
	
	<select id="page" resultMap="authorityOperationResultMap" parameterType="AuthorityOperation">
		select <include refid="table_columns" />
		from authority_operation
		<include refid="page_where" />
		limit #{firstIndex},#{pageSize}
	</select>

    <select id="count" resultType="long" parameterType="AuthorityOperation">
        select count(id) from authority_operation
        <include refid="page_where" />
    </select>

	<!-- 其他自定义SQL -->

	<select id="getAuthOperListByMenuId" resultMap="authorityOperationResultMap" parameterType="long" >
		select <include refid="table_columns" />
		from authority_operation
		where menu_id = #{menuId}
	</select>


	<select id="checkOperUnique" resultType="int" parameterType="AuthorityOperation">
		select count(id) from authority_operation
		<trim prefix="where" prefixOverrides="and | or" >
			<if test="menuId!=null">menu_id = #{menuId} AND</if>
			<if test="id != null and id != -1">id != #{id} AND </if>
			<if test="code != null and code != ''"> CODE = #{code} </if>
		</trim>
	</select>

	
</mapper>